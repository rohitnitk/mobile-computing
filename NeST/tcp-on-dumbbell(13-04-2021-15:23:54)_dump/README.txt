Results
===========================

` NeST ` folder has the code setup and also the `Result` folder 
containing the graphs of ns3 Vegas and Linux Vegas.

`'JsonToText.py'` and `'plot.plt'` files that are used extract cwnd data from `'ss.json'` 
and then plot this data using gnuplot.

Note: offloads aren't disable yet. 
Results with offloads disabled will be uploaded soon.

